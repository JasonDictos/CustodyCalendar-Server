# Use postgres/example user/password credentials
version: '3.1'

services:
  postgresql:
    image: postgres
    environment:
      POSTGRES_PASSWORD: password
      POSTGRES_USER: postgres
    ports:
      - 8888:5432
